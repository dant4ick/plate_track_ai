# Task Completion Checklist

1. Run `flutter analyze` to check for issues
2. Run `flutter test` to verify tests pass
3. If models changed: run `dart run build_runner build`
4. If translations added: update both `en.json` and `ru.json`
